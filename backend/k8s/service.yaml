apiVersion: v1
kind: Service
metadata:
  name: hyperlofy-backend-service
  namespace: hyperlofy
spec:
  type: ClusterIP
  selector:
    app: hyperlofy-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
